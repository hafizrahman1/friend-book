<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>">
    <a href="#" data-dismiss="alert" class="close">x</a>
    <ul>
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>
  <%# <%= content_tag(:div, flash[:error], :id => "flash_error") if flash[:error] %1> %>
  <%# <%= content_tag(:div, flash[:notice], :id => "flash_notice") if flash[:notice] %1> %>
  <%# <%= content_tag(:div, flash[:alert], :id => "flash_alert") if flash[:alert] %1> %>
