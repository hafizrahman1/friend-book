<%= form_for @post, html: { multipart: true } do |f| %>
  <div class="text">
    <%= f.text_area :content, placeholder: "What's on your mind #{current_user.name}?", size: "50X12" %>
  </div><br>
  
  <div class="field">
    <span>Tags:</span>
  <%= f.collection_check_boxes :tag_ids, Tag.all, :id, :name %><br>
    <%= f.fields_for(:tag_attributes) do |tag_form| %>
      New Tag: <%= tag_form.text_field :name, label: :name %><br>
    <% end %>
  </div><br>
  
  <span class="picture">
    <%= f.file_field :photo, accept: 'image/jpeg,image/gif,image/png' %>
  </span><br>

  <div class="actions">
    <%= f.submit "Post", class: "btn btn-primary" %>
  </div>
  
<% end %>
