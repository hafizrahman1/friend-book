<%= form_for @post do |f| %>
  <div class="text">
    <%= f.text_area :content, placeholder: "What's on your mind #{current_user.name}?" %>
  </div>
  <%= f.submit, class: "btn btn-primary" %>
<% end %>
